spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME:taskhub_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  data:
    jpa:
      repositories:
        enabled: true

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:ezEgyNagyonHosszuTitkosKulcsAmitLegalabb256BitHosszuraKellCsinalniHMACSHA256hoz12345}
    expiration-ms: 3600000  # 1 óra

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.taskhub: DEBUG
    org.hibernate.SQL: WARN
